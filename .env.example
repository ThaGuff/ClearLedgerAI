# Optional integrations — copy to .env and fill in
OLLAMA_URL=http://localhost:11434   # your Ollama endpoint (use ngrok URL when on Railway)
OLLAMA_MODEL=llama3.1:8b            # any model you've pulled (e.g. mistral, qwen2.5, etc.)
GHL_WEBHOOK_URL=            # POSTs financial summary after each analysis
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
